{
  "name": "@soracle/circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Circom 2.x + Groth16 circuit families for Soracle (consensus / derivation / predicate)",
  "type": "module",
  "scripts": {
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/setup.sh",
    "export-vkey": "bash scripts/export-vkey.sh",
    "build": "bash scripts/compile.sh && bash scripts/setup.sh && bash scripts/export-vkey.sh",
    "prove:example": "bash scripts/prove-example.sh consensus",
    "test": "node --test test/"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.6"
  },
  "devDependencies": {
    "ffjavascript": "^0.3.0"
  }
}
